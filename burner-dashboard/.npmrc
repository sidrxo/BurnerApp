# NPM Configuration for Burner Dashboard
# Note: DO NOT set optional=false as it breaks native dependencies like lightningcss

# Use hoisted node_modules for smaller size
# (Commented out - can cause issues with some packages)
# node-linker=hoisted

# Enable package-lock for reproducible builds
package-lock=true

# Prefer offline mode when packages are cached
prefer-offline=true
